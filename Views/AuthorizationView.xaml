<UserControl x:Class="Holst.Views.AuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Holst.Views"
             xmlns:UserControls="clr-namespace:Holst.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <Grid Margin="0,10,0,0" Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="LoginTopBtn" Content="Вход" Click="LoginTopBtn_Click" Style="{StaticResource PrimaryButton}" Width="240" Height="70" Grid.Column="0" Margin="0,0,1,0"/>
                    <Button x:Name="RegisterTopBtn" Content="Регистрация" Click="RegisterTopBtn_Click" Width="240" Height="70" Grid.Column="1" Margin="1,0,0,0"/>
                </Grid>
            </Grid>
            <Label Content="Имя пользователя" Foreground="White" FontSize="28" FontWeight="Normal" FontFamily="Calibri"/>
            <TextBox x:Name="Email" Style="{StaticResource Input}"/>
            <Label Content="Пароль" Foreground="White" FontSize="28" FontWeight="Normal" FontFamily="Calibri"/>
            <TextBox x:Name="Password" Style="{StaticResource Input}"/>
            <Label x:Name="PasswordCheckLabel" Content="Вновь пароль" Foreground="White" FontSize="28" FontWeight="Normal" FontFamily="Calibri" Visibility="Collapsed"/>
            <TextBox x:Name="PasswordCheck" Style="{StaticResource Input}" Visibility="Collapsed"/>

            <Button x:Name="ProceedAuth" Click="ProceedAuth_Click" Width="300" Height="80" Content="Войти" Margin="0,5,0,0" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>


    </Grid>
</UserControl>
