<UserControl x:Class="Holst.Views.AuthorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Holst.Views"
             xmlns:UserControls="clr-namespace:Holst.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Rectangle Stroke="#4C4C4C" StrokeThickness="0.2" RadiusX="9" RadiusY="9" Grid.ColumnSpan="2"/>
                    <Button Content="Вход" Style="{StaticResource PrimaryButton}" Width="288" Height="78" Grid.Column="0" Margin="0,0,1,0"/>
                    <Button x:Name="RegisterBtn" Content="Регистрация" Width="288" Height="78" Grid.Column="1" Margin="1,0,0,0"/>
                </Grid>
            </Grid>
            <Label Content="Почта" Foreground="White" FontSize="28" FontWeight="Normal" FontFamily="Calibri"/>
            <TextBox x:Name="Email" Style="{StaticResource Input}"/>
            <Label Content="Пароль" Foreground="White" FontSize="28" FontWeight="Normal" FontFamily="Calibri"/>
            <TextBox x:Name="Password" Style="{StaticResource Input}"/>
            <Label Content="Вновь пароль" Foreground="White" FontSize="28" FontWeight="Normal" FontFamily="Calibri"/>
            <TextBox x:Name="PasswordCheck" Style="{StaticResource Input}"/>

            <Button x:Name="ProceedAuth" Width="300" Height="80" Content="Войти" Margin="0,5,0,0" Style="{StaticResource PrimaryButton}"/>
        </StackPanel>


    </Grid>
</UserControl>
