<UserControl x:Class="Holst.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Holst.Views"
             xmlns:UserControls="clr-namespace:Holst.UserControls"
             mc:Ignorable="d" MinWidth="900" MinHeight="564">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition  />
        </Grid.RowDefinitions>

        <UserControls:HubNavigator Grid.Row="0" Grid.Column="0" Grid.RowSpan="2"/>
        <Path Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
            Stroke="#4C4C4C"
            StrokeThickness="1" 
            Fill="Transparent" 
            Stretch="Fill"
            HorizontalAlignment="Left" VerticalAlignment="Top">
            <Path.Data>
                <PathGeometry>
                    <PathFigure StartPoint="0,565">
                        <LineSegment Point="0, 15"/>
                        <ArcSegment Point="15,0" Size="15,15" SweepDirection="Clockwise"/>
                        <LineSegment Point="15, 0"/>
                        <LineSegment Point="905, 0"/>
                    </PathFigure>
                </PathGeometry>
            </Path.Data>
        </Path>

        <Grid Grid.Row="0" Grid.Column="1" Grid.RowSpan="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <UserControls:Header Grid.Row="0" Panel.ZIndex="1000" />
            <ScrollViewer Grid.Row="1" Margin="15,5,15,15" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Projects}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.OpenProjectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding}"
                                    Style="{StaticResource ProjectCardBtn}"
                                    HorizontalAlignment="Left"
                                    VerticalAlignment="Top">
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" Foreground="White" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Type}" FontSize="12" Foreground="#AAAAAA" Margin="0,4,0,0"/>
                                    <TextBlock Text="{Binding Author}" FontSize="12" Foreground="#888888" Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat='{}{0:dd.MM.yyyy HH:mm}'}" FontSize="10" Foreground="#666666" Margin="0,4,0,0"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl>
